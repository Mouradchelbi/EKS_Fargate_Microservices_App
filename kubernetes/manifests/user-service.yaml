---
apiVersion: v1
kind: Namespace
metadata:
  name: user-service
  labels:
    name: user-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: user-service-sa
  namespace: user-service
  annotations:
    eks.amazonaws.com/role-arn: "REPLACE_WITH_USER_SERVICE_IRSA_ROLE_ARN"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: user-service
  labels:
    app: user-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      serviceAccountName: user-service-sa
      containers:
      - name: user-service
        image: nginx:latest  # Replace with your actual image
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: 500m
            memory: 1Gi
        env:
        - name: SERVICE_NAME
          value: "user-service"
        - name: DB_SECRET_ARN
          value: "REPLACE_WITH_RDS_SECRET_ARN"
        - name: AWS_REGION
          value: "us-east-1"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: user-service
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: user-service
